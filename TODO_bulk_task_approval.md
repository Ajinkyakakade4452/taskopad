# Bulk Task Approval Implementation

## Backend
- [x] 1. Add `POST /api/tasks/approve-bulk` endpoint in `TaskController.java`

## Frontend
- [x] 2. Update `TaskTable.tsx` with multi-select checkboxes + bulk approve/reject buttons
- [x] 3. Update `App.tsx` with bulk handler + pass to TaskTable

## Verification
- [x] 4. Backend compiles successfully (Java backend compiles without errors)
- [x] 5. Frontend typecheck complete (only pre-existing TS errors - none related to this feature)

