# TODO

- [ ] Remove "Quick Access" admin/user quick-fill buttons from LoginPage.tsx (hide by deleting UI and click handlers usage).
- [ ] Ensure LoginPage still compiles (no unused code/variables errors).
- [ ] Run frontend typecheck/build (npm run build or tsc) to confirm.

